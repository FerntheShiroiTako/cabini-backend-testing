{
  "name": "cabinid1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute DB --local --file=./schema.sql",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
